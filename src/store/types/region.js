
/**
 * region store types
 */

const mutationTypes = {
  REGION_SET_REGIONS: 'setRegions'
}
const actionTypes = {
  REGION_SET_REGIONS: 'setRegions'
}

module.exports = {
  mutationTypes,
  actionTypes
}
